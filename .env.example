# The static rule-based chatbot is the default and requires no environment variables.
# Change this to "openai" to restore the OpenAI-backed implementation in a compatible server environment.
VITE_CHAT_MODE=rules

# Keep these server-side values secret. Never prefix them with VITE_.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
